# Day of week parsed appropriately

    Code
      set_day_of_week_factor(NULL, reference_date)
    Condition
      Error in `set_day_of_week_factor()`:
      ! `day_of_week` has an unexpected type. See `?RtGam()`

